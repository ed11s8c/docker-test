sudo: required

language: ruby

services:
  - docker

before_install:
- docker version

jobs:
    include:
        - stage: build
          script:
          - docker build -t docker-git .
          - docker run docker-git
        - stage: release
          branches:
              only:
              - master
          script:
          - docker ps -a
